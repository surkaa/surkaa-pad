export type DiaryEntry = {
    entry_id: string;
    created_at: string; // TODO convert timestamp
    nonce: number[];
}

export type SearchResult = {
    entry_id: string;
    created_at: string; // TODO convert timestamp
    nonce: number[];
}