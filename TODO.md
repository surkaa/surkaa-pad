# TODO

项目级待办与架构债务集中记录在这里。完成后删除对应条目，并在提交信息中说明。

## AI Agent

- [ ] 支持持久化 AI 聊天记录，并设计与日记加密、存储清理和跨设备同步相匹配的数据结构。
- [ ] 为 AI Agent 增加可选的联网搜索工具，启用前明确告知用户会向外部搜索服务发送查询内容。

## 存储架构

- [ ] 将日记专用的对象键、列表、删除、同步和迁移规则重构为通用的加密对象存储模块，集中定义各对象类型的键空间、序列化、加密、版本与生命周期；新模块应支持日记、AI 聊天和密钥相关配置（如密码校验信息、自定义 salt、派生配置），且不得通过伪装为日记对象接入。
