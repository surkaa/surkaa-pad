# TODO

项目级待办与架构债务集中记录在这里。完成后删除对应条目，并在提交信息中说明。

## AI Agent

- [ ] 仅在 Windows 端接入本地部署的 AI Agent，确保日记数据不发送至外部平台，Android 端暂不支持。

## 存储与附件

- [ ] 完善关闭云存储前的本地落盘规划与容量保护（暂缓，等有实际用户需求后再实施）
  - 当前关闭云存储时会先生成云端到本地的同步计划并统计待传输字节，但统计完成后会立即开始下载，用户无法在下载前查看和确认。
  - 增加只读预检查接口，返回云端有效数据总量、本地已有数据量、本次实际下载量、下载/跳过文件数、本地存储位置和该位置所在分区的可用空间；统计应复用现有 ETag 差异计划，不读取对象正文。
  - 设置页先显示“正在统计”，再展示确认弹窗；明确下载位置、预计下载量、可用空间和长时间任务提示。空间不足时阻止关闭云存储，而不是依赖用户自行估算。
  - 所需空间使用保守阈值，例如“待下载字节数 + `max(1 GiB, 5%)`”；真正开始同步前重新生成计划并检查空间，下载过程中仍需正确传播磁盘写满错误。
  - Android 优先针对实际数据目录使用 `StorageManager.getAllocatableBytes()`；Windows 检查实际存储目录所在分区，不应检查固定系统盘或仅显示全局磁盘容量。
  - 当前 LFC 位于 `app_cache_dir()/lfc`，远程模式下可作为缓存，但本地模式下会成为主要持久副本。应评估迁移到 `app_local_data_dir()/lfc`，避免 Android 在空间紧张时回收缓存数据。
  - 旧目录迁移必须幂等且可恢复：优先在同一分区原子重命名，失败时不得删除旧数据；如需复制，应先检查额外空间、流式复制并在完整校验后切换目录。
  - 测试至少覆盖：全新本地目录、部分文件已缓存、ETag 变化、零下载量、容量不足、预检查后计划变化、迁移中断和应用重启恢复。

- [ ] Windows 支持配置本地缓存/数据存储位置（暂缓，等有实际用户需求后再实施）
  - 默认仍使用应用本地数据目录，允许用户选择其他目录或磁盘，以便存放几十 GB 以上的附件。
  - 界面需明确说明：远程模式下该目录主要作为本地缓存，本地模式下则保存唯一的本地完整数据，不能简单称为可随时清理的缓存。
  - 切换位置时先锁定上传、同步和日记写入，统计目标分区空间，流式迁移并校验全部对象；成功后再切换配置，失败则继续使用原目录且不删除原数据。
  - 处理目录无权限、磁盘被移除、路径不可用、目标目录已有冲突数据及应用启动时配置路径失效等情况，并提供恢复到默认位置的入口。

- [ ] 明确 `LocalFileCache` 的职责和命名
  - 本地模式下它是持久化对象存储，远程模式下又承担写透缓存，当前名称无法表达双重职责。
  - 评估重命名为 `LocalObjectStore`，或拆分持久化存储与缓存接口。

- [ ] 收敛分片上传边界
  - multipart 方法仍暴露在 `DiaryStore` trait 中，但实际流程主要由 `attachment_command` 和 `ChunkedUploadState` 驱动。
  - 移除未使用接口，或让分片上传完整地通过 Store 抽象执行，消除当前 Clippy 警告。

- [ ] 加强附件同步有效性校验
  - 当前同步会忽略没有 `manifest.enc` 的孤立目录。
  - 后续应解析源端 manifest，只同步 `attachments` 元数据中存在的附件，避免有效日记目录中的未引用文件被迁移。

- [ ] 将本地附件 HTTP 响应改为真正流式输出
  - 当前服务支持 Range，但会先把单次响应收集为 `Full<Bytes>`；大图片或大范围请求仍可能产生较高内存峰值。

## 配置与构建

- [ ] 收敛云存储状态来源
  - 前端持久化 `remote_enabled`，Rust `AppState` 保存运行时状态；继续减少两者短暂不一致的可能性，并覆盖异常恢复测试。
